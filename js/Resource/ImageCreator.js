let ImageCreator = class ImageCreator {
    static merge(...images) {
        //TODO use previous version filter system to merge images
    }

    static addText(image, text) {
        //TODO use previous version filter system to add text
   }
};

export {ImageCreator};